# Cursor Native QA

Tier: HEAVY - authenticated provider/session and external Cursor integration.
Skills: start-work, teammode, ulw-loop.
Constraints: no pm clear, no uninstall, no saved-cookie/debug-cookie injection, user performs login only, preserve app data.
Success criteria:
- Cursor background refresh uses about:blank/native path without DOM/fallback acceptance.
- Cursor snapshot is saved with real usage lines after preserve-data refresh.
- Any code change is verified by focused tests, assembleDebug, and device refresh evidence.
