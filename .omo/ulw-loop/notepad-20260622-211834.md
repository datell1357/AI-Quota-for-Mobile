# Ultrawork Notepad - Codex collector production baseline restore
Started: 2026-06-22T21:18:34.2012686+09:00

## Plan (exhaustively detailed)
1. Record skills/tier/success criteria and current constraints.
2. Inspect memory and current/prod Codex code paths without touching other providers.
3. Add failing Codex-only tests for production start URL, visible-DOM precedence, weekly time-only reset, 5h past time-only non-rollover, and stale primary resetless payload rejection.
4. Apply smallest Codex-only implementation changes.
5. Re-run focused tests, Android build/test surface, and diff audit for non-Codex drift.

## Success criteria + QA scenarios
C1 production baseline: ProviderRefreshPlan CODEX hidden URL is https://chatgpt.com/ and scanCodexPageState does not reuse stored/network rows ahead of visible DOM. Scenario: targeted unit tests fail RED then pass GREEN; evidence in test output.
C2 reset parser: weekly/spark weekly time-only reset below 1d becomes relative time while 5h past time-only is not rolled to tomorrow. Scenario: targeted JS collector test RED then GREEN.
C3 stale guard: Codex primary/spark-primary visible DOM payload with percent but missing reset is rejected/not saved as trusted usage. Scenario: normalizer or collector unit test RED then GREEN.
C4 regression scope: no non-Codex provider files/branches changed. Scenario: git diff path audit after implementation.
Tier: HEAVY - auth/session WebView external collector path and user explicitly demands careful provider isolation.
Skills: omo:ulw-loop for evidence loop; omo:programming/TDD for code behavior change; test-android-apps:android-emulator-qa if emulator QA is feasible; verification-before-completion before success claims.

## Now
Bootstrap complete; inspecting memory and code.

## Todo
- Inspect memory and current/prod Codex files.
- Write failing tests.
- Implement Codex-only changes.
- Verify focused tests/build and diff scope.

## Findings

## Learnings
- Memory: mobile guardrail says do not hide stale/error states with fake or hardcoded usage and do not classify codex_usage_unavailable/no-payload as auth failure without login redirect or 401/403.
- Current dirty diff adds codexStoredStateRows() and tests stale row priority; this conflicts with requested production baseline.
- Production 21 decompile: G0.java returns https://chatgpt.com/ for CODEX hiddenCollectorUrl; f1.java scanCodexPageState returns extractCodexVisibleDomUsage(accountId).
- RED tests written before production change: ProviderRefreshPlan CODEX root URL, visible DOM over stale rows, active 5h missing reset rejection.
- RED captured: gradle :app:testDebugUnitTest focused run failed 4/4 as expected: CODEX URL still callback URL, scan still row-first, visible DOM not preferred, active 5h missing reset still posted.
- GREEN captured: focused Gradle run passed after Codex-only implementation. Command: gradle :app:testDebugUnitTest with four Codex tests. Exit 0.
- GREEN regression captured: ProviderWebCollectorScriptsTest + ProviderUsageNormalizerTest + ProviderRefreshPlanTest passed. This covers reset parser, 1%/0.1 remaining handling, visible DOM preference, and Codex refresh URL.
- Build captured: gradle :app:assembleDebug exited 0.
- Diff scope note: this turn intentionally touched ProviderRefreshPlan.kt, ProviderWebCollectorScripts.kt, ProviderRefreshPlanTest.kt, ProviderWebCollectorScriptsTest.kt. Pre-existing dirty files remain outside this turn.
- Full test run failed in unrelated/pre-existing dirty session clear tests: ProviderWebSessionClearPolicyTest.explicitDisconnectUsesProviderScopedCookieUrls and WebViewSessionPersistenceTest.sessionDestructiveWebViewAndCookieCallsAreConfinedToExplicitDisconnectCleaner. This turn did not edit those files.
- git diff --check exited 0.
- Post-write review: Changed files are shared/oversized legacy Android files, but edits are scoped to CODEX branch and Codex JS block only. No ProviderUsageNormalizer percent/remaining logic was changed. No one-off Kotlin helper added. No fake/stale fallback added. Full suite residual failures are unrelated pre-existing dirty session-clear policy changes.
